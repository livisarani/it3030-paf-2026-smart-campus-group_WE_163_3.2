# name: CI - Build & Test

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main ]
#   workflow_dispatch:  # Allow manual trigger from GitHub UI

# env:
#   # Paths
#   BACKEND_DIR: ./backend
#   FRONTEND_DIR: ./frontend
  
#   # Database
#   DB_NAME: smart_campus_test
#   DB_USERNAME: root
#   DB_PASSWORD: lavanya

# jobs:
#   # ==================== BACKEND JOBS ====================
#   backend-build-test:
#     name: Backend - Build & Test
#     runs-on: ubuntu-latest  # GitHub runs this on their Linux VM, not your Windows
    
#     services:
#       mysql:
#         image: mysql:8.0
#         env:
#           MYSQL_ROOT_PASSWORD: ${{ env.DB_PASSWORD }}
#           MYSQL_DATABASE: ${{ env.DB_NAME }}
#         ports:
#           - 3306:3306
#         options: >-
#           --health-cmd="mysqladmin ping"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=3
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0
      
#       - name: Set up JDK 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'
#           cache: maven
      
#       - name: Cache Maven dependencies
#         uses: actions/cache@v3
#         with:
#           path: ~/.m2
#           key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
#           restore-keys: ${{ runner.os }}-m2
      
#       - name: Build with Maven
#         run: |
#           cd ${{ env.BACKEND_DIR }}
#           mvn clean compile
      
#       - name: Run Backend Unit Tests
#         run: |
#           cd ${{ env.BACKEND_DIR }}
#           mvn test
#         env:
#           SPRING_PROFILES_ACTIVE: test
#           DB_USERNAME: ${{ env.DB_USERNAME }}
#           DB_PASSWORD: ${{ env.DB_PASSWORD }}
      
#       - name: Package Application
#         run: |
#           cd ${{ env.BACKEND_DIR }}
#           mvn package -DskipTests
      
#       - name: Upload Backend Artifact
#         uses: actions/upload-artifact@v3
#         with:
#           name: backend-jar
#           path: ${{ env.BACKEND_DIR }}/target/*.jar
#           retention-days: 7
      
#       # - name: Generate Test Report
#       #   run: |
#       #     cd ${{ env.BACKEND_DIR }}
#       #     mvn surefire-report:report
      
#       # - name: Upload Test Report
#       #   uses: actions/upload-artifact@v3
#       #   with:
#       #     name: backend-test-report
#       #     path: ${{ env.BACKEND_DIR }}/target/surefire-reports/
#       #     retention-days: 7

#   # ==================== FRONTEND JOBS ====================
#   # frontend-build-test:
#   #   name: Frontend - Build & Test
#   #   runs-on: ubuntu-latest
    
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v3
      
#   #     - name: Set up Node.js
#   #       uses: actions/setup-node@v3
#   #       with:
#   #         node-version: '18'
#   #         cache: 'npm'
#   #         cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json
      
#   #     - name: Install Dependencies
#   #       run: |
#   #         cd ${{ env.FRONTEND_DIR }}
#   #         npm ci
      
#   #     - name: Run Linter (Optional)
#   #       run: |
#   #         cd ${{ env.FRONTEND_DIR }}
#   #         npm run lint || echo "No lint script found"
      
#   #     - name: Run Frontend Tests
#   #       run: |
#   #         cd ${{ env.FRONTEND_DIR }}
#   #         npm test -- --watchAll=false --passWithNoTests --coverage
      
#   #     - name: Build Frontend
#   #       run: |
#   #         cd ${{ env.FRONTEND_DIR }}
#   #         npm run build
#   #         # Create a .nojekyll file to bypass Jekyll processing
#   #         touch build/.nojekyll
      
#   #     - name: Upload Frontend Build
#   #       uses: actions/upload-artifact@v3
#   #       with:
#   #         name: frontend-build
#   #         path: ${{ env.FRONTEND_DIR }}/build/
#   #         retention-days: 7
      
#   #     - name: Upload Coverage Report
#   #       uses: actions/upload-artifact@v3
#   #       with:
#   #         name: frontend-coverage
#   #         path: ${{ env.FRONTEND_DIR }}/coverage/
#   #         retention-days: 7

#   # ==================== DATABASE MIGRATION TEST ====================
#   database-test:
#     name: Database Migration Test
#     runs-on: ubuntu-latest
#     needs: [backend-build-test]
    
#     services:
#       mysql:
#         image: mysql:8.0
#         env:
#           MYSQL_ROOT_PASSWORD: ${{ env.DB_PASSWORD }}
#           MYSQL_DATABASE: ${{ env.DB_NAME }}
#         ports:
#           - 3306:3306
#         options: >-
#           --health-cmd="mysqladmin ping"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=3
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
      
#       - name: Set up JDK 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'
      
#       - name: Test Database Migrations
#         run: |
#           cd ${{ env.BACKEND_DIR }}
#           mvn spring-boot:run &
#           sleep 30
#           curl -f http://localhost:8080/actuator/health || true
#         env:
#           SPRING_PROFILES_ACTIVE: test
#           DB_USERNAME: ${{ env.DB_USERNAME }}
#           DB_PASSWORD: ${{ env.DB_PASSWORD }}

#   # ==================== INTEGRATION TESTS ====================
#   # integration-test:
#   #   name: Integration Tests
#   #   runs-on: ubuntu-latest
#   #   needs: [backend-build-test, frontend-build-test]
    
#   #   services:
#   #     mysql:
#   #       image: mysql:8.0
#   #       env:
#   #         MYSQL_ROOT_PASSWORD: ${{ env.DB_PASSWORD }}
#   #         MYSQL_DATABASE: ${{ env.DB_NAME }}
#   #       ports:
#   #         - 3306:3306
#   #       options: >-
#   #         --health-cmd="mysqladmin ping"
#   #         --health-interval=10s
#   #         --health-timeout=5s
#   #         --health-retries=3
    
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v3
      
#   #     - name: Set up JDK 17
#   #       uses: actions/setup-java@v3
#   #       with:
#   #         java-version: '17'
#   #         distribution: 'temurin'
      
#   #     - name: Download Backend Artifact
#   #       uses: actions/download-artifact@v3
#   #       with:
#   #         name: backend-jar
#   #         path: ${{ env.BACKEND_DIR }}/target/
      
#   #     - name: Start Backend Server
#   #       run: |
#   #         cd ${{ env.BACKEND_DIR }}
#   #         java -jar target/*.jar &
#   #         sleep 30
      
#   #     - name: Run API Tests with Newman (Postman)
#   #       run: |
#   #         npm install -g newman
#   #         newman run docs/postman-collection.json \
#   #           --environment docs/postman-environment.json \
#   #           --reporters cli,json \
#   #           --reporter-json-export test-results.json
#   #       continue-on-error: true
      
#   #     - name: Upload API Test Results
#   #       uses: actions/upload-artifact@v3
#   #       with:
#   #         name: api-test-results
#   #         path: test-results.json
#   #         retention-days: 7

#   # ==================== NOTIFICATION ====================
#   notify:
#     name: Notification
#     runs-on: ubuntu-latest
#     needs: [backend-build-test, frontend-build-test, integration-test]
#     if: always()
    
#     steps:
#       - name: Check build status
#         run: |
#           if [ "${{ needs.backend-build-test.result }}" == "success" ] && \
#              [ "${{ needs.frontend-build-test.result }}" == "success" ]; then
#             echo "✅ CI Pipeline Completed Successfully!"
#           else
#             echo "❌ CI Pipeline Failed!"
#             exit 1
#           fi